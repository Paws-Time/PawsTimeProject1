import React from "react";

export const SearchForm = ({ title }: { title: string }) => {
  return <form>
    <h2>{title}</h2>
    <select>

    </select>
    <input />
    <button>검색</button>
  </form>
};

{/* <SearchForm title="게시판" />
<SearchForm title="스토어" /> */}